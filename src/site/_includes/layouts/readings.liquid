<!doctype html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta charset="utf-8">
        <meta name="Description" content="{{ description }}" />
        <title>Insight Center - Readings</title>
    </head>
    <body>
    <article>

        <h1>{{ pageTitle }}</h1>

        {{ content }}

        <small>{{ attribution }}<small>
    </article>
    </body>
</html>